# Per-run test artefacts — regenerated by tests/Invoke-AllTests.ps1
tests/output/

# Editor / OS
.vs/
.vscode/.history/
*.swp
Thumbs.db
.DS_Store

# Backup snapshots (config edits leave these around)
*.bak
*.bak.*

# Secrets — never commit (token lives in $env:USERPROFILE\OneDrive - Kritical Pty Ltd\Github-SecretsOutsideOfGitRepos)
*.token
*-secret.txt
*-secrets.json
secrets/

# Build artefacts
out/
dist/
*.nupkg
*.zip

# Pester run artifact
testResults.xml

# --- Generated Lens/tool artefacts (added 20260726, estate-gitignore lane) ---
# Regenerable machine output. Re-emit with the Lens sweep commands in AGENTS.md.
# Hand-authored docs/ findings and scripts/*.ps1 are deliberately NOT ignored.
# Lens sweep run output: timestamped, regenerable
scripts/output/

# Kritical repo lane-lease (see Kritical.PS.GitHub Enter-KritRepoLease) -- never commit
.kritical-lane-lock.json
